# SmartTag worker — example environment. Copy to apps/worker/.env.
NODE_ENV=development
LOG_LEVEL=info

# Redis (BullMQ). docker compose exposes 127.0.0.1:56379.
REDIS_URL=redis://127.0.0.1:56379
WORKER_CONCURRENCY=4
