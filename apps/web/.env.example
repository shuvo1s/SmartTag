# SmartTag web — example environment. Copy to apps/web/.env.local.
# Where the Next.js server forwards /api/v1/* (the NestJS API). Server-side only, read at runtime.
API_INTERNAL_URL=http://127.0.0.1:4000
